services:
  db:
    image: postgres:17-alpine
    restart: unless-stopped
    ports:
      - "5432:5432"
    environment:
      POSTGRES_DB: comptes
      POSTGRES_USER: comptes
      POSTGRES_PASSWORD: comptes_dev
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:

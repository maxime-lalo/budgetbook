# Provider : "postgresql" (défaut) ou "sqlite"
# DB_PROVIDER="sqlite"

# PostgreSQL (défaut)
DATABASE_URL="postgresql://comptes:comptes_dev@localhost:5432/comptes?schema=public"

# SQLite (décommenter pour utiliser SQLite)
# DATABASE_URL="file:./dev.db"
